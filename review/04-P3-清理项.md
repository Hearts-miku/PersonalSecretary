# P3 — 清理项

---

## 1. Gradle wrapper 已生成但未提交 ⚠️

本地工作区已有 `gradlew`、`gradlew.bat`、`gradle/wrapper/`，但 `origin/main` 里**没有**
（`git ls-tree` 无命中，本地为未跟踪状态）。

因此干净克隆和 CI 仍然构建不了。这是**一个 `git add` 的距离**，却挡着：

- 本审计始终无法编译验证
- CI 无法建立
- 每轮提交前的自查（三次回归里的第 3 次，4 个编译错误，本可被一次构建全部拦下）

`gradle.properties` 本地也有未提交的修改，需要一并确认。

---

## 2. 单 provider 收敛后的清理（决策 [D-1](06-修复顺序建议.md)）

产品已决定只保留自定义 OpenAI 兼容 provider，删除 Gemini / Anthropic 两条路径。
涉及范围：

| 项 | 位置 |
|---|---|
| `callGeminiApi` | [GeminiRepository.kt:96](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/app/src/main/java/com/example/data/ai/GeminiRepository.kt#L96) |
| `callAnthropicApi` | [GeminiRepository.kt:228](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/app/src/main/java/com/example/data/ai/GeminiRepository.kt#L228) |
| `generateContent` 的 `when (provider)` 分发 | [:87-89](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/app/src/main/java/com/example/data/ai/GeminiRepository.kt#L87) |
| `getEffectiveApiKey` 的 `provider == "GEMINI"` 分支 + `BuildConfig` 回退 | [:36-41](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/app/src/main/java/com/example/data/ai/GeminiRepository.kt#L36) |
| `getEffectiveBaseUrl` 的 provider 分支 | [:47-55](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/app/src/main/java/com/example/data/ai/GeminiRepository.kt#L47) |
| `UserSettingsEntity.apiProvider` 字段 | [Entities.kt:43](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/app/src/main/java/com/example/data/local/Entities.kt#L43) |
| secrets-gradle-plugin + `.env` / `.env.example` | [app/build.gradle.kts:8,81-84](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/app/build.gradle.kts#L81) |
| `import com.example.BuildConfig` | [GeminiRepository.kt:3](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/app/src/main/java/com/example/data/ai/GeminiRepository.kt#L3) |
| `metadata.json` 的 `MAJOR_CAPABILITY_SERVER_SIDE_GEMINI_API` | [metadata.json](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/metadata.json) |
| 类名 `GeminiRepository` 已名不副实 | 建议改为 `AiRepository` 之类 |

**注意点**：

- 删除 `apiProvider` 字段是 schema 变更。因应用未发布，可以**直接重新生成 `6.json`**
  （或重置版本号），不必新增 6→7 迁移——与 [P1-15](03-P2-健壮性与体验.md) 的处置一起决定
- 删除 Anthropic 路径会连带移除其 `stop_reason` 截断检测与 `max_tokens = 8192`；
  OpenAI 路径的 `finish_reason == "length"` 检测必须保留
- `buildConfig = true` 若无其他用途也可关闭

---

## 3. 误提交的文件

### `fix_type.py`（仓库根目录）

[fix_type.py](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/fix_type.py) —— 一次性脚本，用于修补 `Type.kt` 的一处逗号语法错误。
第 4 轮提交进来，历经五轮仍在。应直接删除。

---

## 4. 孤儿代码

| 项 | 说明 |
|---|---|
| `exportAllDataToZip` 全链路 | 三层齐备但无 UI 入口，见 [P2-19](03-P2-健壮性与体验.md)。**不要删——应该接上 UI** |
| `callGeminiApi` / `callAnthropicApi` | 新用户不可达，按 [D-1](06-修复顺序建议.md) 删除 |

---

## 5. 冗余调用

`writeCareerProfile` 在 `triggerAISummarize` 中被调用两次
（[:206](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/app/src/main/java/com/example/data/repository/WorkLogRepository.kt#L206) 与
[:218](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/app/src/main/java/com/example/data/repository/WorkLogRepository.kt#L218)），
写入同一份内容。幂等所以无害，但应去掉其一。

---

## 6. 模板残留

| 项 | 位置 |
|---|---|
| `namespace = "com.example"` | [app/build.gradle.kts:13](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/app/build.gradle.kts#L13) —— 保留示例前缀，`BuildConfig`、`R` 都落在此包下 |
| `proguard-rules.pro` | [app/proguard-rules.pro](https://github.com/Hearts-miku/PersonalSecretary/blob/7679b62/app/proguard-rules.pro) —— 全文注释 |
| `ExampleUnitTest.kt` | 断言 `2 + 2 == 4`，已有真测试，可删 |

主题名已修正 ✅；`Type.kt` 已补全 ✅；死字段与死 DAO 已清理 ✅；`cancelEditing*` 已接线 ✅

---

## 7. 构建配置

- **Compose BOM 版本落差** —— `composeBom = "2024.09.00"` 搭配 AGP 9.1.1 / compileSdk 36.1 /
  Kotlin 2.2.10，落后约两年
- **release 未开启混淆压缩** —— `isMinifyEnabled = false`
- **`kotlin.compiler.execution.strategy=in-process` 被注释掉** —— 第 3 轮的改动，未说明理由
- **`logging-interceptor` 仍在依赖中但未挂载** —— 保持现状，接上会把 API Key 与用户全部
  工作内容写进 logcat
