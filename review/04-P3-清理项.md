# P3 — 清理项

---

## 1. D-1 的两处残留

单 provider 收敛执行得很彻底——全项目搜索 `gemini` / `anthropic` 只剩两处：

| 项 | 位置 | 说明 |
|---|---|---|
| `MAJOR_CAPABILITY_SERVER_SIDE_GEMINI_API` | [metadata.json](https://github.com/Hearts-miku/PersonalSecretary/blob/2589cdd/metadata.json) | 声明的能力已不存在 |
| `.env` | 仓库根目录（仍被跟踪） | secrets 插件已移除，该文件不再被读取 |

`.env.example` 同理。secrets 插件在
[app/build.gradle.kts](https://github.com/Hearts-miku/PersonalSecretary/blob/2589cdd/app/build.gradle.kts) 中已注释掉并留了说明注释，
可以连同这两个文件一起清掉。

---

## 2. 孤儿 schema 文件

`app/schemas/` 下有 `5.json` 与 `6.json`，而数据库版本已是 7 且
`exportSchema = false`——两个文件已经对不上号。

处置取决于 [P0-6 回归](02-P1-功能与安全.md) 的决定：若恢复 `exportSchema = true`，
应生成 `7.json` 并删除失效的 5/6；若维持关闭，则整个目录都该删掉，避免误导。

---

## 3. 孤儿代码

上一轮的孤儿已全部处理：`exportAllDataToZip` 已接上 UI ✅、
`cancelEditing*` 已接线 ✅、`fix_type.py` 已删除 ✅、`ExampleUnitTest.kt` 已删除 ✅。

当前无新增孤儿代码。

---

## 4. 冗余调用

`writeCareerProfile` 在 `triggerAISummarize` 中被调用两次，写入同一份内容。
幂等所以无害，但应去掉其一。

---

## 5. 模板残留

| 项 | 位置 |
|---|---|
| `namespace = "com.example"` | [app/build.gradle.kts:13](https://github.com/Hearts-miku/PersonalSecretary/blob/2589cdd/app/build.gradle.kts#L13) —— 保留示例前缀，`BuildConfig`、`R` 都落在此包下 |
| `proguard-rules.pro` | [app/proguard-rules.pro](https://github.com/Hearts-miku/PersonalSecretary/blob/2589cdd/app/proguard-rules.pro) —— 全文注释 |

主题名已修正 ✅；`Type.kt` 已补全 ✅；死字段与死 DAO 已清理 ✅

---

## 6. 构建配置

**Gradle wrapper 已提交** ✅ —— `gradlew`、`gradlew.bat`、`gradle/wrapper/` 齐备，
干净克隆现在可以构建，CI 可以建立。这是本轮最有价值的一项基础设施改进。

### 剩余

- **Compose BOM 版本落差** —— `composeBom = "2024.09.00"` 搭配 AGP 9.1.1 /
  compileSdk 36.1 / Kotlin 2.2.10，落后约两年
- **release 未开启混淆压缩** —— `isMinifyEnabled = false`
- **`kotlin.compiler.execution.strategy=in-process` 被注释掉** —— 第 3 轮的改动，
  未说明理由；该行原本用于规避「Could not connect to Kotlin compile daemon」
- **`logging-interceptor` 仍在依赖中但未挂载** —— 保持现状，接上会把 API Key 与用户
  全部工作内容写进 logcat
- **`buildConfig = true`** —— `BuildConfig.GEMINI_API_KEY` 已随 D-1 移除，若无其他用途
  可关闭
