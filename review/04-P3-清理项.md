# P3 — 清理项

---

## 1. 死代码

| 项 | 位置 | 说明 |
|---|---|---|
| `triggerAISummarizeForUnsummarizedOrSelected()` | [WorkLogViewModel.kt:175](https://github.com/Hearts-miku/PersonalSecretary/blob/c5a0225/app/src/main/java/com/example/ui/viewmodel/WorkLogViewModel.kt#L175) | 🆕 全项目只有定义，零调用 |
| `AiRepository` 的 `CryptoManager` import | [AiRepository.kt](https://github.com/Hearts-miku/PersonalSecretary/blob/c5a0225/app/src/main/java/com/example/data/ai/AiRepository.kt) | 随 N-20 修复后正文已不引用 |

上一轮的孤儿已全部处理：`exportAllDataToZip` 已接 UI ✅、`cancelEditing*` 已接线 ✅、
`fix_type.py` / `ExampleUnitTest.kt` 已删除 ✅、`.env` 系列已删除 ✅。

---

## 2. D-1 最后一处残留

`metadata.json` 仍声明 `MAJOR_CAPABILITY_SERVER_SIDE_GEMINI_API`
（[metadata.json](https://github.com/Hearts-miku/PersonalSecretary/blob/c5a0225/metadata.json)）——该能力随 D-1 已不存在。

全项目除此之外已搜不到 `gemini` / `anthropic` 的任何引用。

---

## 3. proguard 规则中的失效条目

[app/proguard-rules.pro](https://github.com/Hearts-miku/PersonalSecretary/blob/c5a0225/app/proguard-rules.pro) 本轮补齐了 Room / 协程 /
Keystore 相关规则，是必要的准备。但其中：

```
-keep class com.squareup.moshi.** { *; }
```

moshi 依赖早已随 D-1 之前的清理移除，这条规则已无对应目标。

另外 `isMinifyEnabled = false`（[app/build.gradle.kts:52](https://github.com/Hearts-miku/PersonalSecretary/blob/c5a0225/app/build.gradle.kts#L52)），
**这批规则目前不生效**——属于发布前清单的一部分，见
[06-修复顺序建议.md](06-修复顺序建议.md)。

---

## 4. 冗余调用

`writeCareerProfile` 在 `triggerAISummarize` 中被调用两次，写入同一份内容。
幂等所以无害，但应去掉其一。注意这一处与 [N-22](03-P2-健壮性与体验.md)（取消中途打断）
位于同一区段，一并处理更省事。

---

## 5. 模板残留

| 项 | 位置 |
|---|---|
| `namespace = "com.example"` | [app/build.gradle.kts:13](https://github.com/Hearts-miku/PersonalSecretary/blob/c5a0225/app/build.gradle.kts#L13) —— 保留示例前缀，`R` 类落在此包下 |

主题名已修正 ✅；`Type.kt` 已补全 ✅；死字段与死 DAO 已清理 ✅；
`buildConfig = false` 已确认全项目无 `BuildConfig` 引用 ✅

---

## 6. 构建配置

已完成：Gradle wrapper 提交 ✅、`exportSchema = true` 与 `7.json` ✅、
`buildConfig = false` ✅、proguard 规则补齐 ✅、`.env` 系列清除 ✅。

### 剩余

- **Compose BOM 版本落差** —— `composeBom = "2024.09.00"` 搭配 AGP 9.1.1 /
  compileSdk 36.1 / Kotlin 2.2.10，落后约两年
- **`isMinifyEnabled = false`** —— 见上文第 3 节
- **`kotlin.compiler.execution.strategy=in-process` 被注释掉** —— 第 3 轮的改动，
  未说明理由；该行原本用于规避「Could not connect to Kotlin compile daemon」
- **`logging-interceptor` 仍在依赖中但未挂载** —— 保持现状，接上会把 API Key 与用户
  全部工作内容写进 logcat
