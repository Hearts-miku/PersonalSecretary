# P3 — 清理项

---

## 1. 误提交的文件

### `fix_type.py`（仓库根目录）

**位置**：[fix_type.py](https://github.com/Hearts-miku/PersonalSecretary/blob/fa9c62c/fix_type.py)

```python
with open("app/src/main/java/com/example/ui/theme/Type.kt", "r") as f:
    text = f.read()
text = text.replace("letterSpacing = 0.5.sp,\n      )\n        titleLarge", ...)
```

一次性脚本，用于修补 `Type.kt` 中的一处逗号语法错误。第 4 轮提交进来，至今仍在。
应删除——它对项目没有任何持续价值，且暴露了当时 `Type.kt` 处于语法错误状态。

---

## 2. 缺失的构建产物

### `app/schemas/6.json`

数据库版本已是 6，`exportSchema = true` 且 `room.schemaLocation` 已配置到
`$projectDir/schemas`，但目录下**只有 `5.json`**。

构建时 Room 会自动生成 `6.json`——文件缺失说明**最近几轮改动都没有构建过**。这同时
是 [P1-15](02-P1-功能与安全.md)（迁移未经验证）最直接的佐证。

---

## 3. 孤儿代码

| 项 | 位置 | 说明 |
|---|---|---|
| `cancelEditingProfile` / `cancelEditingWorkExp` / `cancelEditingProjectExp` | [WorkLogViewModel.kt:338,358,378](https://github.com/Hearts-miku/PersonalSecretary/blob/fa9c62c/app/src/main/java/com/example/ui/viewmodel/WorkLogViewModel.kt#L338) | 三个都零调用。**不要删——应该接到 UI 上**，见 [N-8](03-P2-健壮性与体验.md) |
| `exportAllDataToZip` 全链路 | MarkdownFileManager / Repository / ViewModel | 三层齐备但无 UI 入口，见 [P2-19](03-P2-健壮性与体验.md) |
| 第一个 `init` 块 | [WorkLogViewModel.kt:18](https://github.com/Hearts-miku/PersonalSecretary/blob/fa9c62c/app/src/main/java/com/example/ui/viewmodel/WorkLogViewModel.kt#L18) | 被第二个 init 完全覆盖，可删 |

上一轮的孤儿 `clearRawNotesContent()` 已删除 ✅

---

## 4. 冗余调用

`writeCareerProfile` 在 `triggerAISummarize` 中被调用两次
（[:206](https://github.com/Hearts-miku/PersonalSecretary/blob/fa9c62c/app/src/main/java/com/example/data/repository/WorkLogRepository.kt#L206) 与
[:218](https://github.com/Hearts-miku/PersonalSecretary/blob/fa9c62c/app/src/main/java/com/example/data/repository/WorkLogRepository.kt#L218)），
写入的是同一份内容。幂等所以无害，但应去掉其一。

---

## 5. 模板残留

| 项 | 位置 | 说明 |
|---|---|---|
| `namespace = "com.example"` | [app/build.gradle.kts:13](https://github.com/Hearts-miku/PersonalSecretary/blob/fa9c62c/app/build.gradle.kts#L13) | `com.example` 是保留示例前缀，`BuildConfig`、`R` 都落在此包下 |
| `proguard-rules.pro` | [app/proguard-rules.pro](https://github.com/Hearts-miku/PersonalSecretary/blob/fa9c62c/app/proguard-rules.pro) | 全文注释，无任何规则 |
| `ExampleUnitTest.kt` | [app/src/test/java/com/example/ExampleUnitTest.kt](https://github.com/Hearts-miku/PersonalSecretary/blob/fa9c62c/app/src/test/java/com/example/ExampleUnitTest.kt) | 断言 `2 + 2 == 4`。已有真测试，可删 |

主题名已在第 4/5 轮修正为 `Theme.WorkLogResume` ✅；`Type.kt` 已补全三种排版样式 ✅

---

## 6. 未被调用的 DAO 方法

`getPendingTodos`、`updateTodo`、`clearCompleted`、`getLogFlowByDate`、`getUnsummarizedLogs`、
`deleteByDate` 已在第 4 轮删除 ✅

本轮新增的 `deleteAllLogs` / `deleteAllTodos` / `deleteAllProfiles` / `deleteAllVersions`
均被 `clearAllData` 使用 ✅

---

## 7. 构建配置

已完成：Firebase / Retrofit / Moshi 依赖注释掉；`moshi.kotlin.codegen` KSP 处理器注释掉；
`googleServices.missing.passthrough` 移除；`room.schemaLocation` 配置到位。

### 剩余

- **无 Gradle wrapper** —— `gradle/` 下只有 `libs.versions.toml`。构建版本不可复现，CI 无法
  固定，也是本审计始终无法编译验证的原因。建议补一个匹配 AGP 9.1.1 的 wrapper——
  这同时能解决 `6.json` 缺失和 [P1-15](02-P1-功能与安全.md) 的验证问题。
- **Compose BOM 版本落差** —— `composeBom = "2024.09.00"` 搭配 AGP 9.1.1 / compileSdk 36.1 /
  Kotlin 2.2.10，落后约两年。
- **release 未开启混淆压缩** —— `isMinifyEnabled = false`。
- **`.env` 机制会把 key 打进 APK** —— 发布版本建议完全依赖应用内设置的 key。
- **`kotlin.compiler.execution.strategy=in-process` 被注释掉** —— 第 3 轮的改动，未说明理由。
  该行原本用于规避「Could not connect to Kotlin compile daemon」。
- **`logging-interceptor` 仍在依赖中但未挂载** —— 保持现状，不要接上。
