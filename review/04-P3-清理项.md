# P3 — 清理项

---

## 1. 残留（上轮起未处理）

| 项 | 位置 |
|---|---|
| `metadata.json` 仍声明 `MAJOR_CAPABILITY_SERVER_SIDE_GEMINI_API`（D-1 最后残留） | [metadata.json](https://github.com/Hearts-miku/PersonalSecretary/blob/e543020/metadata.json) |
| proguard 中 `-keep class com.squareup.moshi.**`，moshi 依赖早已移除 | [app/proguard-rules.pro](https://github.com/Hearts-miku/PersonalSecretary/blob/e543020/app/proguard-rules.pro) |
| `AiRepository` import 了 `CryptoManager` 但正文不再使用 | [AiRepository.kt](https://github.com/Hearts-miku/PersonalSecretary/blob/e543020/app/src/main/java/com/example/data/ai/AiRepository.kt) |

本轮已处理：`triggerAISummarize` 中重复的 `writeCareerProfile` 调用已去除 ✅；
上轮列出的死代码 `triggerAISummarizeForUnsummarizedOrSelected()` 现在转调新的批量整理，
有了实际语义，不再是死代码 ✅。

---

## 2. 本轮新增

- **`extractTodos` 解析读取了 prompt 从未要求的 `project` 字段**——prompt 要求模型输出
  `category`，解析器却先尝试 `obj.optString("project")`。无害的死回退，可删。
- **旧分类值会出现在项目筛选里**——此前 prompt 让模型输出「工作 / 学习 / 个人」，
  筛选 chip 只排除了「工作」「Work」「通用项目」，历史数据中的「学习」「个人」会显示为「项目」。
  未发布前提下影响仅限开发数据。

---

## 3. 构建配置

- **Compose BOM 版本落差** —— `2024.09.00` 搭配 AGP 9.1.1 / Kotlin 2.2.10，落后约两年
- **`isMinifyEnabled = false`** —— proguard 规则已备好但当前不生效
- **`kotlin.compiler.execution.strategy=in-process` 被注释掉** —— 未说明理由
- **`logging-interceptor` 仍在依赖中但未挂载** —— 保持现状
- **`namespace = "com.example"`** —— 示例前缀，`R` 类落在此包下
