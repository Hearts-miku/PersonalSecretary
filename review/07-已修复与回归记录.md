# 已修复与回归记录

追踪跨轮次的状态变化。

---

## 一、已确认修复（64 项）

### P0（12 项，全部解决）

P0-1 ~ P0-5（首轮的构建阻断与三条数据丢失通路、测试源集不编译）、
P0-6（破坏性迁移，**第 9 轮回归，见 [02-P1](02-P1-功能与安全.md)**）、
P0-7 / P0-8（第 4 轮引入的编译阻断）、
P0-9 ~ P0-12（第 6 轮引入的四个编译错误）。

### P1（20 项，全部解决）

| 编号 | 问题 | 修法 |
|---|---|---|
| P1-1 | 重复总结导致待办重复插入 | `deletePendingTodosForDate` |
| P1-2 | 待办排序把 HIGH 排到最后 | `CASE priority WHEN 'HIGH' THEN 0 ...` |
| P1-3 | 构建期 key 发给其他厂商 | 随 D-1 整条路径移除 |
| P1-4 | API Key 明文存储 | **本轮**：Android Keystore + AES-256-GCM，`v2:` 前缀向下兼容 |
| P1-5 | key 放在 URL query | 随 D-1 移除 |
| P1-6 | 日报注入无体积预算 | `getRecentLogsContext` 字符预算 |
| P1-7 | 消毒破坏用户内容 | 收缩到只做标签转义 + 长度截断 |
| P1-8 | `career_profile.md` 无备份 | 覆写前快照 `type = "PROFILE"` |
| P1-9 | 空 AI 输出照样落库 | `isBlank()` 校验并中止 |
| P1-10 | 截断信号未检测 | OpenAI `finish_reason` 保留（其余随 D-1 移除） |
| P1-11 | 模型名无效、o3 temperature 冲突 | o1/o3 保护保留 |
| P1-12 | 日期格式化用 `Locale.getDefault()` | 全部改 `Locale.ROOT` |
| P1-13 | 切换主题丢弃未保存的 API 配置 | `rememberSaveable` + null 加载守卫 |
| P1-14 | 档案编辑内容旋屏即丢 | 提到 ViewModel |
| P1-15 | Room 迁移未经验证 | 随 `MIGRATION_5_6` 删除而作废 |
| P1-16 | 语义搜索裸拼 prompt | **本轮**：查询词与日志分别消毒并包标签 + 18000 字预算 |
| P1-17 | 占位域名成为功能性默认值 | **本轮**：默认值改空 + 三项配置各自提前失败 + 占位符仅作 `placeholder` |
| N-1 ~ N-6 | 见历次记录 | 已解决 |

### P2（24 项）

P2-1、P2-2（部分）、P2-3、P2-4、P2-6、P2-8、P2-9、P2-10、P2-11、P2-14、P2-16、P2-18、
P2-19、P2-21、P2-22，以及 N-7 ~ N-12、N-15 ~ N-17。

本轮新解决：**P2-4**（日历跨月同步）、**P2-19**（导出 UI 接上
`ActivityResultContracts.CreateDocument`）、**P2-22**（简历风格持久化）、
**N-15**（`takeWhile` 改累加式）、**N-16**（清空后刷新统计）、**N-17**（提炼失败保留内容）、
**P2-7 的待办配色部分**。

### P3

依赖清理、死字段与死 DAO 清理、主题重命名、`Type.kt` 补全、`cancelEditing*` 接线、
`loadMdInfo` 接线、**Gradle wrapper 提交**、**`fix_type.py` 删除**、
**`ExampleUnitTest.kt` 删除**。

---

## 二、已执行的决策

### D-1 删除 Gemini / Anthropic 路径 ✅ 第 9 轮完成

`GeminiRepository` → `AiRepository`，两条路径删除，secrets 插件与 `BuildConfig` 依赖移除，
`apiProvider` 字段删除。

**挑拣准确**：审计曾特别标注不要连带删掉仍有用的保护，核对结果 OpenAI 的
`finish_reason == "length"`（[AiRepository.kt:131](https://github.com/Hearts-miku/PersonalSecretary/blob/2589cdd/app/src/main/java/com/example/data/ai/AiRepository.kt#L131)）
与 o1/o3 的 `temperature` 保护（[:108](https://github.com/Hearts-miku/PersonalSecretary/blob/2589cdd/app/src/main/java/com/example/data/ai/AiRepository.kt#L108)）
均完整保留。

残留两处：`metadata.json` 的 Gemini capability 声明、仍被跟踪的 `.env`。
见 [04-P3](04-P3-清理项.md)。

### D-2 `MIGRATION_5_6` 的去留 ✅ 第 9 轮选择 (b)

删除迁移、版本重置为 7、改用 `fallbackToDestructiveMigration()`。未发布前提下成立，
但引出了 [P0-6 回归](02-P1-功能与安全.md)。

---

## 三、回归事件记录（四次）

| 事件 | 轮次 | 内容 | 成因 | 状态 |
|---|---|---|---|---|
| 1 | 第 2 轮 | 回退 8 项已修复的问题 | 基于陈旧文件副本编辑后整体覆盖 | 第 3 轮已恢复 |
| 2 | 第 4 轮 | P1-13 回归 | 两个约束被当成互斥 | 第 6 轮已恢复 |
| 3 | 第 6 轮 | 4 个编译错误 | 未实际构建 | 第 7 轮已修复 |
| 4 | 第 9 轮 | `exportSchema` 由 `true` 改回 `false` | 随 D-2 一并调整数据库配置时波及 | **待处理** |

事件 4 与前三次性质不同——它是一次有意的配置调整的副作用，而非疏漏。破坏性迁移部分
是 D-2 的正常执行；但 `exportSchema` 属于可以独立保留的能力，
详见 [02-P1](02-P1-功能与安全.md)。

### 规避方式

1. **改动后构建一次** —— wrapper 已提交，阻碍已清除
2. 改动前先 `git fetch && git rebase origin/main`
3. 提交后自查 `git diff <上一次修复提交> HEAD -- <改动文件>`
4. **补测试** —— 唯一能自动拦截事件 1、2 类问题的手段

---

## 四、测试现状

唯一的测试仍是
[`AISafetyManagerTest`](https://github.com/Hearts-miku/PersonalSecretary/blob/2589cdd/app/src/test/java/com/example/AISafetyManagerTest.kt)，3 条用例。
本轮删除了 `ExampleUnitTest.kt`（断言 `2 + 2 == 4`）。

**Gradle wrapper 已提交，`./gradlew :app:testDebugUnitTest` 现在随时可跑。**

### 建议补充（按优先级）

| 目标 | 锁住的发现 | 优先级 |
|---|---|---|
| `CryptoManager` encode/decode 往返；明文与 `v1:` 向下兼容 | [P1-4](#) / [N-19](03-P2-健壮性与体验.md) | **最高** |
| Base URL / API Key / 模型名为空时不发出请求 | [P1-17](#) | **最高** |
| `sanitizeUserInput` 的 `tagName` 分支 | N-6 | 高 |
| `extractJsonSubstring` — 前置文字、嵌套括号、字符串内括号 | P2-1（曾被回退） | 高 |
| OpenAI `finish_reason` 截断检测 | P1-10 | 中 |
| `removeRawNotesForDate` — 多段落、仅剩一段、无匹配 | P0-3 | 中 |
| `DiffUtils.computeDiff` | P2-12 | 低 |

注意 `CryptoManager` 在 JVM 单测环境下 `getOrCreateSecretKey()` 会返回 null（代码注释
已说明），因此往返测试实际验证的是 `v1:` 降级路径。要覆盖 `v2:` 需要 instrumented test。

---

## 五、轮次记录

| 轮次 | 提交 | 结果 |
|---|---|---|
| 首轮审计 | `d381dde` | 发现 P0 6 项、P1 14 项、P2 20 项 |
| 第 1 轮 | `9987fc9` | 修复 16 项 |
| 第 2 轮 | `b4735eb` | **回退 8 项**（事件 1） |
| 第 3 轮 | `884ed39` | 恢复全部回退；**首个真测试落地** |
| 第 4 轮 | `f02ac83` | 真实 Migration、导出后端；**2 个编译阻断**；P1-13 回归（事件 2） |
| 第 5 轮 | `fa9c62c` | 补齐编译阻断；新增 3 个功能；**P0 首次清零** |
| 第 6 轮 | `378a406` | 一次修 13 项；**4 个编译错误**（事件 3） |
| 第 7 轮 | `47d4f26` | 修复全部编译错误；完成 P2-3 |
| 第 8 轮 | `7679b62` | 提交 `6.json`；收敛为单 provider；新增 P1-17 |
| 第 9 轮 | `2589cdd` | **一次完成 6 个批次**；P1 全部清零；`exportSchema` 回归（事件 4） |
