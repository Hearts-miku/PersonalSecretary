# P1 — 功能缺陷与安全问题

**当前 0 项。这是首轮审计以来 P0 与 P1 首次同时清零。**

---

## 本轮解决

### P0-6（回归）已收口

`exportSchema` 从 `false` 恢复为 `true`，`7.json` 生成并提交，失效的 `5.json` /
`6.json` 删除，`fallbackToDestructiveMigration()` 改用非废弃的
`fallbackToDestructiveMigration(dropAllTables = true)`。

审计上一轮建议把这条拆成两半——「现在恢复 schema 导出、发布前再撤销破坏性迁移」，
本轮正是按这个拆法执行的。可分离且有累积成本的那一半已经做完，剩下的一半进入
发布检查清单（见 [01-P0-阻断项.md](01-P0-阻断项.md)）。

`7.json` 已与
[实体定义](https://github.com/Hearts-miku/PersonalSecretary/blob/c5a0225/app/src/main/java/com/example/data/local/Entities.kt)逐列核对一致，
`user_settings` 表已不含随 D-1 删除的 `apiProvider` 列。

---

## 历史 P1 全部解决

20 项 P1 的完整清单见 [07-已修复与回归记录.md](07-已修复与回归记录.md)。
最近三轮解决的重点项：

| 编号 | 内容 |
|---|---|
| P1-4 | API Key 真加密（Keystore + AES-256-GCM，`v2:` 前缀向下兼容） |
| P1-16 | 语义搜索纳入标签隔离约定，标签使用零例外 |
| P1-17 | 占位域名不再作为功能性默认值；三项配置各自提前失败 |

---

## 接下来的重心

P1 清零后，剩余工作集中在两处：

1. **[03-P2](03-P2-健壮性与体验.md) 的 10 项** —— 其中 [N-21](03-P2-健壮性与体验.md) 与
   [N-22](03-P2-健壮性与体验.md) 是本轮取消机制带来的，优先级最高
2. **发布检查清单** —— 见 [06-修复顺序建议.md](06-修复顺序建议.md)
