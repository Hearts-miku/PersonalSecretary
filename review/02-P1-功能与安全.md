# P1 — 功能缺陷与安全问题

**当前 0 项。** 自第 10 轮起保持清零。

20 项历史 P1 的清单见 [07-已修复与回归记录.md](07-已修复与回归记录.md)。

---

## 本轮新功能的安全面检查

本轮新增的两处 AI 调用均已核对，**未发现 P1 级问题**：

| 功能 | 核对结果 |
|---|---|
| `testConnection`（[AiRepository.kt:82](https://github.com/Hearts-miku/PersonalSecretary/blob/e543020/app/src/main/java/com/example/data/ai/AiRepository.kt#L82)） | prompt 为固定文本，不含用户内容；API Key 取自设置页输入框，不落库、不打日志 |
| `extractTodos` prompt 重写 | 用户笔记仍经 `sanitizeUserInput` 并包在 `<user_raw_content>` 内，标签约定保持零例外 |

提示词隔离层面唯一值得注意的是 [N-24](03-P2-健壮性与体验.md)：它不是标签缺口，而是把
AI 的输出重新当作「用户原文」送回模型，削弱了护栏 prompt 中「只根据用户真实记录提炼」的前提。
按当前影响归为 P2。
