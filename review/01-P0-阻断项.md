# P0 — 阻断项

**当前 0 项。**

---

## 历史 P0 全部清零

首轮的 6 项、第 4 轮的 2 项、第 6 轮的 4 项，至今全部解决。
详见 [07-已修复与回归记录.md](07-已修复与回归记录.md)。

第 6 轮（`378a406`）引入的 4 个编译错误已在第 7 轮（`47d4f26`）修复：

| 编号 | 问题 | 修法 |
|---|---|---|
| P0-9 | `sanitizeUserInput(..., tagName = ...)` 参数不存在 | 函数签名增加 `tagName: String = "user_raw_content"` |
| P0-10 | `CryptoManager.encrypt/decrypt` 已改名，调用点未改 | 三处改为 `encode` / `decode` |
| P0-11 | `Icons.Default.Close` 未导入 | 补 `androidx.compose.material.icons.filled.Close` |
| P0-12 | `rememberSaveable` 未导入 | 补 `androidx.compose.runtime.saveable.rememberSaveable` |

---

## 需要留意的降级项

[P0-6](07-已修复与回归记录.md)（破坏性迁移）在第 4 轮已实质解决——
`fallbackToDestructiveMigration` 移除，换成
[`MIGRATION_5_6`](https://github.com/Hearts-miku/PersonalSecretary/blob/47d4f26/app/src/main/java/com/example/data/local/AppDatabase.kt#L30)。

但这把失败模式从「静默清库」变成了「硬崩溃」，而迁移**从未被执行验证过**。
该风险单列为 [P1-15](02-P1-功能与安全.md)，是当前优先级最高的未完成项。
