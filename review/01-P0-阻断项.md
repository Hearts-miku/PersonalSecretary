# P0 — 阻断项

**当前 0 项。** 历史上的 12 项 P0 全部解决，详见 [07-已修复与回归记录.md](07-已修复与回归记录.md)。

---

## 发布前仍需处理

数据库当前是 `fallbackToDestructiveMigration(dropAllTables = true)`
（[AppDatabase.kt:37](https://github.com/Hearts-miku/PersonalSecretary/blob/e543020/app/src/main/java/com/example/data/local/AppDatabase.kt#L37)）。
未发布阶段是合理取舍；`exportSchema = true` 与 `7.json` 已就位，写迁移的能力保住了。
发布前需换成显式 `Migration`，见 [06-修复顺序建议.md](06-修复顺序建议.md) 的发布检查清单。
